// Array Destructuring
const girlfriends = ["sita", "barsa", "nurse", "geeta"];

const [one, two, three] = girlfriends;

//  const {one, two} = girlfriends;


console.log( one + "&" + two + "&" + three + " are the gf of nripesh.");

console.log(`${one} & ${two} & ${three} are the gf of nripesh.`);
